<% if @mess_type.errors.any? %>
<h2><%= pluralize(@mess.errors.count, "error") %> prevented this mess type from being saved:</h2>
<ul>
  <% @mess_type.errors.full_messages.each do |msg| %>
  <li><%= msg %></li>
  <% end %>
</ul>
<% end %>

<%= form_with model: [:admin, @mess_type], local: true do |f| %>
  <div class="field">
    <%= f.label :name, class: "label" %>
    <div class="control">
      <%= f.text_field :name, required: true, class: "input" %>
    </div>
  </div>

  <p>
    <%= f.submit class: "button is-primary" %>
  </p>
<% end %>
